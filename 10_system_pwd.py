import os

path = os.getcwd()
print(path) #OUTPUT= /absolute/path  path is now saved in the variable

wrongSolution = os.system('pwd') #output= 0  the path will be printed in the terminal but WON'T be added to the variable

# look at file 14_modules-libraries for more info 

