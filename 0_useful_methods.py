#26 of the TOP methods for data types in Python
# * the method works on somme primitive and iterable data types
# Note that in Python Strings are immutable, meaning this

##---------For List Data Types
# 1. append() - adds an element to the end of the list
# 2. extend() - adds all elements of a list to the end of another list
# 3. insert() - inserts an element at a given index
# 4. remove() - removes an element from the list
# 5. pop() - removes and returns an element at a given index
# 6. index() - returns the index of the first element with the specified value
# 7. *len() - return the length of the list 
# 8. *reverse() - reverses the order of the original list
# 8.A) *reversed()- return a reversed iterator object and doesn't modify the original list 
# 9. sort() - sorts the list in ascending order
# 9.A) *sorted() - sorts the list in ascending order and doesn't change the original 
# 10. copy() - returns a shallow copy of the list

# 11. clear() - removes all elements from the list
# 12. sum() - returns the sum of all elements in the list
# 13. *min() - returns the smallest element in the list
# 14. count() - returns the number of elements with the specified value
# 15. *max() - returns the largest element in the list
# 16. count() - returns the number of elements with the specified value
# 17. join() - Joins the elements of an iterable to the end of a string.
# 18. slice()  - Returns a slice object representing the set of indices specified by range(start, stop, step).
# 19. *enumerate()- Returns an enumerate object. It contains the index and value of all the items of the iterable as a tuple.

# 20. *filter()- Constructs an iterator from elements of an iterable for which a function returns true.
# 21. *map()- This method applies a function to every element in an iterable object (such as a list, tuple, or dictionary) and returns a list of the results
# 22. *reduce() - This method applies a function of two arguments cumulatively to the elements of an iterable object, from left to right, so as to reduce the iterable to a single value.
# 23. any() - This method returns True if any element of an iterable is True.
# 24. all()  - This method returns True if ALL elements of an iterable are True.
# 25. *zip() -This method takes two or more iterables and returns an iterator of tuples.
# 26. islice() - This method returns an iterator that returns selected items from the given iterable. It takes three arguments: an iterable, a start index, and a stop index. It returns the items from the iterable starting from the start index up to, but not including, the stop index.
# 27. format() - allows you to insert placeholders in a string and then replace them with values. This is useful for creating strings with dynamic content.

